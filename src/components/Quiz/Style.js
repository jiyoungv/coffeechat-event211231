import styled from 'styled-components';

export const Quizs = styled.section`
    .quiz-area {
        padding: 30px 0;
    }

    .quiz-area-inner {
        max-width: 375px;
        margin: 0 auto;
        padding: 0 20px;
    }
`;